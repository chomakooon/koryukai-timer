# PROJECT

この再構成版は、破損バックアップから実行可能な Vite + React + TypeScript プロジェクトとして復元したものです。

## 復元時の方針

- 元のフォルダ構成を可能な限り維持
- 足りなかった `ui/*` コンポーネントはローカル実装で補完
- 未使用 import / state は整理
- すぐ `npm install && npm run dev` できる形を優先
